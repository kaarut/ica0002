$TTL	604800
{{ domain }}.{{ zone }}.	IN	SOA	vm1.{{ domain }}.{{ zone }}. kaarut.{{domain}}.{{zone}}. (
			      2		; Serial
			 604800		; Refresh
			  86400		; Retry
			2419200		; Expire
			 604800 )	; Negative Cache TTL
;
{{domain}}.{{zone}}.	IN	NS	vm1

{}

vm1	IN	A	192.168.42.166
vm2	IN	A	192.168.42.170